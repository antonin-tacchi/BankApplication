server:
  port: 8071

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/antonin-tacchi/bankapplication-config
          clone-on-start: true
          default-label: main
